/**
 * Dados fixos da CONTRATADA, usados no contrato gerado.
 * Edite aqui e publique de novo (git commit + push) sempre que precisar
 * mudar algo — não existe painel de configurações nesta versão simples.
 */
export const COMPANY_CONFIG = {
  name: "DI FESTAS E DECORAÇÕES",
  address: "Rua Antônio Bandeira, nº 87, Aquiraz/CE",
  representativeName: "",
  representativeCpf: "",
  /**
   * Logo exibida no cabeçalho do contrato. Salve o arquivo em
   * src/assets/logo-difestas.png e troque a linha abaixo por:
   *   import companyLogoDataUri from "./assets/logo-difestas.png?inline";
   * (o sufixo ?inline faz o Vite embutir a imagem como data URI, o que é
   * necessário porque o contrato é aberto como um Blob HTML isolado, sem
   * acesso confiável a caminhos relativos do site.)
   */
  logoDataUri: "",
};
